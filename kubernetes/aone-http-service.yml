apiVersion: v1
kind: Service
metadata:
  name: my-service
spec:
  type: NodePort
  selector:
    run: aone-pod
  ports:
      # By default and for convenience, the `targetPort` is set to the same value as the `port` field.
      - port: 80
        targetPort: 3050
        nodePort: 30080
